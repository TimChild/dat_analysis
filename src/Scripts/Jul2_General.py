from src.Scripts.StandardImports import *
from src.Scripts.Jun30_dot_tuning import _plot_dat


if __name__ == '__main__':
    # dat = get_dat(46)
    # fig, ax = plt.subplots(1)
    # ax: plt.Axes
    # ax.cla()
    # x = dat.Data.x_array
    # y = dat.Data.Exp_current_dc_2d  # type: np.ndarray
    #
    #
    # PF.waterfall_plot(x, y, ax, y_spacing=-1, label=True, index=[f'{y:.0f}' for y in dat.Data.y_array])
    # ax.set_xlabel(dat.Logs.x_label)
    # ax.set_ylabel('Current /nA')
    # ax.set_title('Waterfall plot of RCSS vs RCSQ')
    # ax.legend(title='RCSS /mV')

    import time

